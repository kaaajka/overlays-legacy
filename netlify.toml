[build]
  command = "corepack enable && corepack prepare pnpm@10.17.1 --activate && pnpm install --frozen-lockfile && pnpm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.16.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
